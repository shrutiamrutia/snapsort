import React from "react";

import Navigation from "./Navigation";

const Header = (props) => {
    return (
        <div>

            <Navigation />
        </div>
    );
};

export default Header;
